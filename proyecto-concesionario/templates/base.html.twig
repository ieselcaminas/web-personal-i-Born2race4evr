<!DOCTYPE html>
<html lang="es">
{% block stylesheets %}{% endblock %}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Proyecto Automoción{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <link rel="icon" href="{{ asset('favicon.png') }}" type="image/png">
    <style>
        body {
            background-color: #f5f6fa;
        }
        .navbar {
            margin-bottom: 20px;
        }
        footer {
            text-align: center;
            padding: 10px;
            background: #222;
            color: #fff;
            margin-top: 50px;
        }
        .navbar-animated {
            background: linear-gradient(90deg, #007bff, #6610f2, #6f42c1, #007bff);
            background-size: 300% 300%;
            animation: navbarGradient 8s ease infinite;
        }

        @keyframes navbarGradient {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .navbar-animated .nav-link,
        .navbar-animated .navbar-brand {
            color: #fff !important;
            font-weight: 500;
            transition: color 0.3s;
        }

        .navbar-animated .nav-link:hover {
            color: #ffc107 !important;
        }

        .table-themed {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
            background: rgba(255, 255, 255, 0.05);
        }

        .table-themed thead {
            background: linear-gradient(90deg, #007bff, #6610f2, #6f42c1) !important;
            color: #fff !important;
        }

        .table-themed thead th {
            border: none !important;
            text-align: center;
            color: #fff !important;
            background: transparent !important;
        }

        .table-themed tbody tr {
            transition: background 0.3s, transform 0.2s;
        }

        .table-themed tbody tr:hover {
            background: rgba(255, 255, 255, 0.15);
            transform: scale(1.01);
        }

        .table-themed tbody td {
            text-align: center;
            vertical-align: middle;
            border-top: 1px solid rgba(255, 255, 255, 0.2);
            padding: 12px;
        }

        .table-themed .btn {
            font-size: 0.9rem;
            padding: 5px 10px;
            border-radius: 8px;
        }

    </style>
</head>
<body>
{% if app.request.attributes.get('_route') != 'home' %}
<nav class="navbar navbar-expand navbar-dark navbar-animated">
    <div class="container">
        <a class="navbar-brand" href="/">Automoción</a>
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav me-auto">
                <li class="nav-item"><a class="nav-link" href="{{ path('marca_index') }}">Marcas</a></li>
                <li class="nav-item"><a class="nav-link" href="{{ path('coche_index') }}">Coches</a></li>
            </ul>
        </div>
    </div>
</nav>
{% endif %}

<div class="container mt-4">
    {% block body %}{% endblock %}
    {% for message in app.flashes('success') %}
    <div class="alert alert-success" role="alert">
        {{ message }}
    </div>
{% endfor %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
